<%= render 'shared/cover', user: @user %>
<br>
<div id="user-<%= @user.id %>" class="row">
  <div class="col-lg-3">
    <%= render 'shared/user_info' %>
  </div>
  <div class="col-lg-6">
    <div id="activities">
      <% if @activities.empty? %>
        <div class="well">
          <%= render('shared/no_resource', resources: 'Activities') %>
        </div>
      <% else %>
        <%= render_activities(@activities, layout: '/shared/activity') %>
      <% end %>
    </div>
    <%= render 'shared/paginate', resources: @activities %>
  </div>
  <div class="col-lg-3">
    <% if is_current_user?(@user) %>
      <div class="well">
        <ul class="nav nav-pills nav-stacked">
          <li><%= link_to 'Επεξεργασία προφίλ', edit_user_path(@user) %></li>
          <li><%= link_to 'Αλλαγή κωδικού', edit_user_registration_path %></li>
          <li><%= link_to 'Φίλοι', friends_user_path(@user) %></li>
          <li><%= link_to 'Ακόλουθοι', followers_user_path(@user) %></li>
          <li><%= link_to 'Απανεργοποίηση', deactivate_user_path(@user) %></li>
        </ul>
      </div>
    <% end %>
  </div>
</div>